# --- Server ---
PORT=5000
NODE_ENV=development

# --- CORS ---
# Trebuie să fie ORIGIN-ul frontend-ului Vite din local (portul 5173)
CORS_ORIGIN=http://localhost:5173

# --- Bază de date (local) ---
DATABASE_URL=postgresql://postgres:Flaviaisfamily.1606@localhost:5432/artizani?schema=public

# --- Auth/secure ---
JWT_SECRET=FlaviaisFamily.1712

# --- App/meta (pentru linkuri în email în dev e util să fie frontendul local) ---
APP_URL=http://localhost:5173
EMAIL_FROM=ArtFest <no-reply@siteul-tau.ro>

# --- SMTP (dev / mock sau real dacă ai) ---
SMTP_HOST=smtp.tld
SMTP_PORT=587
SMTP_USER=smtp-user
SMTP_PASS=smtp-pass
RESET_TOKEN_TTL_MINUTES=60

# --- Plăți (dummy în dev) ---
PAYMENTS_PROVIDER=dummy

# (opțional) Neutilizat în codul tău — poți șterge
# API_BASE_URL=http://localhost:5000

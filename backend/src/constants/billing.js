// fallback dacă vendor nu are plan activ
export const DEFAULT_COMMISSION_BPS = 1000; // 10%

// recomandat: payout după livrare sau după x zile
export const PAYOUT_DELAY_DAYS = 3;

// dacă vrei payout doar când shipment e DELIVERED
export const PAYOUT_ONLY_AFTER_DELIVERY = true;
